<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="RestPlanner.MaterialThemeOverlay" parent="Theme.RestPlanner" />

    <style name="RestPlanner.ActionBarThemeOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- title text color -->
        <item name="android:textColorPrimary">?attr/colorTextOnBottomSheetDialog</item>
    </style>

    <style name="RestPlanner.BottomSheetDialog" parent="Theme.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/RestPlanner.BottomSheet</item>
        <!-- Themes for components. -->
        <item name="android:textViewStyle">@style/RestPlanner.TextView</item>
        <item name="radioButtonStyle">@style/RestPlanner.RadioButton</item>
        <item name="checkboxStyle">@style/RestPlanner.CheckBox</item>
        <item name="switchStyle">@style/RestPlanner.Switch</item>
    </style>

    <style name="RestPlanner.BottomSheet" parent="Widget.MaterialComponents.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/RestPlanner.ShapeAppearanceOverlay</item>
        <item name="backgroundTint">?attr/colorBottomSheetDialog</item>
    </style>

    <style name="RestPlanner.ShapeAppearanceOverlay" parent="">
        <item name="cornerSize">0dp</item>
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">@dimen/corner_size</item>
        <item name="cornerSizeTopRight">@dimen/corner_size</item>
    </style>

    <style name="RestPlanner.TextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <style name="RestPlanner.RadioButton" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <item name="android:textSize">@dimen/rb_text_size</item>
        <item name="android:padding">@dimen/rb_padding</item>
        <item name="android:textColor">?attr/colorTextOnBottomSheetDialog</item>
        <item name="materialThemeOverlay">@style/RestPlanner.MaterialThemeOverlay</item>
    </style>

    <style name="RestPlanner.CheckBox" parent="Widget.MaterialComponents.CompoundButton.CheckBox">
        <item name="android:textColor">?attr/colorTextOnBottomSheetDialog</item>
        <item name="android:textSize">16sp</item>
        <item name="materialThemeOverlay">@style/RestPlanner.MaterialThemeOverlay</item>
    </style>

    <style name="RestPlanner.Switch" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <item name="android:ems">12</item>
        <item name="android:textColor">?attr/colorTextOnBottomSheetDialog</item>
        <item name="android:textSize">16sp</item>
        <item name="materialThemeOverlay">@style/RestPlanner.MaterialThemeOverlay</item>
    </style>
</resources>